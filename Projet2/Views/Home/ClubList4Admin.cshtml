@*//@model List<Projet2.Models.Club>*@
@model Projet2.ViewModels.CreateClubViewModel

@{
    ViewBag.Title = "ClubList";
    Layout = "_Layout";
}


<body>
        <center>
            <h1>Liste des clubs</h1>
            <label for="name">Recherche</label>
            <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Recherche par nom..." class="btnPara btnPara--btnClub">

            <table class="tableau-style" id="ListClubsTable">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom du club</th>
                        <th>Adresse</th>
                        <th>adresse Email</th>
                        <th>Mot de passe</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var member in Model.Clubs)

                    {
                        <tr>
                            <td>@member.Id</td>
                            <td>@member.InfosClub.NomClub</td>
                            <td>@member.InfosClub.Adresse.NumeroRue, @member.InfosClub.Adresse.NomRue - @member.InfosClub.Adresse.CodePostal @member.InfosClub.Adresse.NomVille </td>
                            <td>@member.Compte.AdressEmail</td>
                            <td>@member.Compte.MotDePasse</td>
                        </tr>
                    }
                </tbody>
            </table>

            <script>function myFunction() {
                    // Declare variables
                    var input, filter, table, tr, td, i, txtValue;
                    input = document.getElementById("myInput");
                    filter = input.value.toUpperCase();
                    table = document.getElementById("ListClubsTable");
                    tr = table.getElementsByTagName("tr");

                    // Loop through all table rows, and hide those who don't match the search query
                    for (i = 0; i < tr.length; i++) {
                        td = tr[i].getElementsByTagName("td")[1]; // indicates the column for the filtering
                        if (td) {
                            txtValue = td.textContent || td.innerText;
                            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                                tr[i].style.display = "";
                            } else {
                                tr[i].style.display = "none";
                            }
                        }
                    }
                }</script>


            <form action="RemoveClub" method="get">
                <div style="margin: 20px">
                    <input type="text" name="id" placeholder="Id" size="10rem" />
                    <button type="submit" class="btnPara btnPara--btnClub">Supprimer club</button>
                </div>
            </form>

            <form action="ModifyClub" method="get">
                <div style="margin: 20px">
                    <input type="text" name="id" placeholder="Id" size="10rem" />
                    <button type="submit" onclick="window.location.href='https://localhost:5001/Home/ModifyClub';" class="btnPara btnPara--btnClub">Modifier club</button>
                </div>
            </form>



        <a href=@("/Home/EspaceAdmin/"+Model.Utilisateur.InfosPersonnelles.Nom)><button class="btnParaBis btnParaBis--btnBis">Bon retour</button></a>


        <div style="margin: 20px">
            <button type="submit" onclick="window.location.href='https://localhost:5001/Home/CreateClub';" class="btnParaBis btnParaBis--btnBis">Créer club</button>
        </div>
        <div>

            <input type="button" onclick="window.location.href='https://localhost:5001/Home/EspaceAdmin';" value="Retour Espace Admin" class="btnParaBis btnParaBis--btnBis"/>
        </div>
      </center>
    </body>


